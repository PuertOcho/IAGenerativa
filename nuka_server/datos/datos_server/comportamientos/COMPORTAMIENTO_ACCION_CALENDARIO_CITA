Instrucciones: Rellena el siguiente JSON con la siguiente frase, la fecha actual es $DATE
Estructura:
{
     "etiqueta": $ETIQUETA,
     "fecha": $FECHA
} 
$ETIQUETA: Etiqueta que resume el texto
$FECHA: Teniendo en cuenta que la fecha actual es $DATE (año-mes-dia hora:minutos:segundos), el parametro $FECHA se rellena con el mismo formato teniendo en cuenta el texto, si no se especifica hora, las horas, minutos y segundos se formatean como 00:00:00 y si no se indica lo contrario la hora indicada es por la tarde	
Responde solo con el JSON: